SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (SID_NAME = PLSExtProc)
      (ORACLE_HOME = <%= node['oracle_xe']['server_home'] %>)
      (PROGRAM = extproc)
    )
    (SID_DESC =
      (SID_NAME = CLRExtProc)
      (ORACLE_HOME = <%= node['oracle_xe']['server_home'] %>)
      (PROGRAM = extproc)
    )
    (SID_DESC =
      (SID_NAME = XE)
      (ORACLE_HOME = <%= node['oracle_xe']['server_home'] %>)
    )
  )

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC_FOR_XE))
      (ADDRESS = (PROTOCOL = TCP)(HOST = <%= node['oracle_xe']['host'] %>)(PORT = <%= node['oracle_xe']['listener_port'] %>))
    )
  )

DEFAULT_SERVICE_LISTENER = (XE)
